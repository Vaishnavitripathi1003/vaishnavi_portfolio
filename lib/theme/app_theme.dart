import 'package:flutter/material.dart';

class AppColors {
  static const bg = Color(0xFF0A0A08);
  static const surface = Color(0xFF111110);
  static const surface2 = Color(0xFF1A1A18);
  static const border = Color(0xFF222220);
  static const accent = Color(0xFFC8F060);
  static const accent2 = Color(0xFFF0C860);
  static const text = Color(0xFFF0EDE6);
  static const muted = Color(0xFF888880);
  static const tagBg = Color(0x1AC8F060);
}

class AppSpacing {
  static const xs = 4.0;
  static const sm = 8.0;
  static const md = 16.0;
  static const lg = 24.0;
  static const xl = 40.0;
  static const xxl = 64.0;
}
